-- Auto-loot destination container config. Swapping which item acts as the
-- Loot Bag is a one-line change here — see BUILD_NOTES.md for why item 2004
-- (golden backpack) was picked (unused anywhere else in this dataset).
LootBag = {
	itemId = 2004
}
